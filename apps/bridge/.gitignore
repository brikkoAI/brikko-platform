# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.eggs/
*.egg
MANIFEST

# Virtualenvs
.venv/
venv/
env/

# Tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
.tox/

# Editors
.vscode/
.idea/
*.swp
.DS_Store

# Bridge-specific runtime data — must NEVER end up in git
data/
*.db
*.db-journal
*.db-wal
*.db-shm

# Bot environment files (contain Telegram tokens, daemon URLs)
bot.env
.env
.env.local

# Auth tokens — written by daemon at runtime
auth_tokens.json
*.auth_tokens.json

# Supervisor logs
supervisor.log
*.brikko-bridge/

# SSH key paths (if anyone foolishly puts a key in here)
brikko_aeza
brikko_aeza.pub
*.key
*.pem

# Pytest tmpdir leftovers (Windows quirk)
.pytest-of-*/
