# Bot-only deps — daemon's fastapi/uvicorn intentionally absent.
aiogram>=3.13,<4
httpx>=0.27,<1
pydantic>=2.9,<3
pydantic-settings>=2.5,<3
aiosqlite>=0.20
structlog>=24.4
# Bot doesn't strictly need psutil (it only ever calls daemon /processes
# over HTTP), but pinning here keeps requirements files consistent if we
# ever decide to run daemon-and-bot in one container for testing.
psutil>=6.0
# Phase 8 — bot subscribes to bridge:approval-request and writes the user's
# button-click response back to bridge:approval-response:<id>.
redis>=5.0,<6
