"""Cross-cutting middleware-style helpers.

Distinct from ``voltari_gateway/auth/`` (which is auth-specific) so we
don't mix orthogonal concerns. Keep modules small and importable
without side effects.
"""
